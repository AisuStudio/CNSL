"use client";

// Note Pad tool — placeholder for Phase A (nav). The real notes UI (folder
// tree + markdown editor) lands in Phase B.
export default function NotePad() {
  return (
    <div style={{ padding: "24px", color: "var(--color-text-muted)" }}>
      Note Pad — coming next (folders + markdown).
    </div>
  );
}
